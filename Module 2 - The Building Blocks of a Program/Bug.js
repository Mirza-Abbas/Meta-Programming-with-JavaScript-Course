//When a bug happens in a program it keeps running but in an unintended way.

function addTwoNum(a, b){
    console.log(a + b);
}

addTwoNum("A", 1);      //the function is intended to add two numbers but it still runs
